{
  "name": "@ecom/platform-observability",
  "version": "0.1.0",
  "private": true,
  "description": "Platform observability package (empty scaffold, Story 1.1).",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  }
}
