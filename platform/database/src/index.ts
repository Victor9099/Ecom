// @ecom/platform-database — PostgreSQL + Prisma baseline (AD-25).
// The generated client lives in platform/database/generated/prisma (gitignored)
// and is produced by `prisma generate` (run via `pnpm build` in this package).
// No model types exist yet because Story 1.1 forbids business tables.
export {};
