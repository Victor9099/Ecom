{
  "name": "@ecom/module-pricing",
  "version": "0.1.0",
  "private": true,
  "description": "Pricing bounded context (empty scaffold, Story 1.1).",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  }
}
