{
  "name": "@ecom/module-catalog",
  "version": "0.1.0",
  "private": true,
  "description": "Catalog bounded context (empty scaffold, Story 1.1).",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  }
}
