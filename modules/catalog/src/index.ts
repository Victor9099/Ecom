// @ecom/module-catalog — Catalog bounded context (AD-1 single owner).
// Empty scaffold: contracts/application/domain/adapters surfaces are reserved
// and contain no business tables or speculative domain entities (Story 1.1 AC).
export {};
