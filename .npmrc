# Ecom — npm/pnpm configuration.
# Node engine mismatch is a warning, not a hard failure, so installs on a
# transition host (e.g. Node 24.18.0 while `engines.node` documents >=24.19.0)
# still succeed. The authoritative pin lives in `.nvmrc` and `package.json#engines`.
engine-strict=false