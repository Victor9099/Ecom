{
  "name": "@ecom/ui",
  "version": "0.1.0",
  "private": true,
  "description": "Shared accessible visual primitives (no domain behavior, AD-21).",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  }
}
