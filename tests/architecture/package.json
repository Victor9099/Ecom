{
  "name": "@ecom/tests-architecture",
  "version": "0.1.0",
  "private": true,
  "description": "Architecture boundary and ownership checks (executable AD-15 surface).",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "6.0.3"
  }
}
